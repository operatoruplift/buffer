# Buffer production release — September 20, 2026

## Current live-risk and monitoring extension

The current pass adds a direct public Velocity live-risk journey, adjacent current maintenance context, independent saved-rule management, and protected hosted monitoring with exactly one Discord adapter. Existing Meridial/B4/B6 media, narrated films, scenario arithmetic, reports, branding and PWA remain intact. `/demo` and the product descriptions identify the new features and preserved film dates accurately.

[PR1](https://github.com/operatoruplift/buffer/pull/1) merged after [release CI](https://github.com/operatoruplift/buffer/actions/runs/35463066014) passed. Source revision `69098443963fed4f28a7777cec3c318fc534ed41` deployed as `dpl_3tkiBM8LH57rkGwJa7MKM4kPt3Uk`, `https://buffer-o1eatte2z-operatoruplift.vercel.app`; Vercel confirmed Ready with the canonical `https://bufferonsolana.vercel.app` alias.

- **535 unit tests in 24 suites**, lint, typecheck and production build passed. Actual PostgreSQL verification passed **131 assertions**: 55 legacy, 48 hosted runtime, 28 scheduler contract checks.
- **180 deterministic browser cases passed; 2 real-auth cases skipped** because confirmed test-account credentials are absent. The initial CI found a desktop PWA test interception race; it was reproduced, fixed by registering interception before worker activation, reviewed, and verified across 40 repeated desktop/mobile PWA cases. All privacy assertions remain and no service-worker behavior was changed.
- Before/after captures at 1440×1000 and 390×844 preserve all four required routes. Final production captures show HTTP 200, no JS/console/network errors, no horizontal overflow, and matching blue wordmarks. Both production live journeys selected the sole discovered account automatically, kept current risk unchanged while adjusting the scenario, and downloaded matching dated reports.
- Production checks passed seven public routes, seven logo/favicon/media assets and five private/invalid-request cases. Fresh Velocity at `2026-09-19T19:09:32.067Z` returned two USDT-settled positions with current USD maintenance context at slots `448497796`/`448497797`. Pacifica returned 22 USD scenario positions. Jupiter returned one inventory-only SOL-PERP row with modeled:false.
- `/demo` explains the current build and links into the live explorer. All September 13 narrated films, captions and transcripts remain intact with explicit historical context. The Meridial hero, B4 sequence, B6 refraction, PWA, report compatibility, ribbon-B identity and installed icons remain preserved.
- Hosted migrations `20260919183521` and `20260919185033` applied. Vercel production worker/cron secrets are configured; sends remain disabled and notification mode is test. The private synchronous HEAD helper uses Vault without a public request queue. The named Supabase minute job `buffer-monitor-minute` is active. Three actual scheduled runs at `2026-09-19T19:10:00Z`, `19:11:00Z` and `19:12:00Z` succeeded, each producing a completed protected worker journal entry in **dry_run** mode. There are zero owner rules/events; this proves scheduler operation, not a recipient delivery.

The final media follow-up fixes a confirmed entrance-animation hitbox jump: hero actions fade without translating, while the title/body rise and keyboard reveal remain. Three native-animation regressions cover pointer, keyboard and keyboard-to-pointer input. Native caption tracks now prepare in hidden mode before playback, preserving the viewer's existing caption choice and all video/resume controls. This removes the observed contention when rapidly testing the three films sequentially. Direct HTTP/2+Brotli checks matched the original caption hashes; the files themselves were intact. Account selection tests now wait for discovery to expose one of its supported controls. Earlier post-merge test failures and their diagnoses remain in the evidence directory; no failure is hidden by retries or optional CI.

The final local production build passed lint/typecheck/build, 18 design/media cases, 18 repeated account-selection cases and 6 repeated sequential three-film journeys. The additional regressions remain part of required CI.

The final release pointer is the annotated Git tag `buffer-2026-09-20-verified`. Its annotation records the final source revision, production deployment identity and completed checks, avoiding a self-referential commit hash in this document. The initial hosted activation and provider observations above remain separately dated.

Independent code, TypeScript, backend security and scheduler reviewers approved the changes. Evidence lives under `work/evidence/real-alerts-2026-09-20/`, including CI logs, production API checks, private endpoint checks, scheduled-run records, screenshots and `production/production-motion-1440.webm`. The 23-second recording shows advancing hero frames, board travel/reversal, caption wipe, B4 animation and B6 changing canvas pixels; its complete playback was verified. [The integration matrix](BUFFER-INTEGRATION-STATUS.md) separates implemented, fixture-tested and live-verified states.

**Open gate:** no Discord channel/webhook/owner mapping has been supplied and no real send has been authorized. Outbound acceptance and matching channel receipt are **not run**. Real-auth credentialed browser proof and SMTP signup/recovery remain unverified; public email stays disabled. Conditional liquidation prices remain deliberately excluded pending a verified full stress model.

Rollback target immediately before this release: `dpl_5wdmjFko3qqd7qZYRP8vzcePMnXX`, `https://buffer-qcnuy1zoe-operatoruplift.vercel.app`. Pause the named cron job and keep sends false before promoting it; retain the additive schema, RLS and event history.

## Previous release baseline


The user explicitly authorized continuing and deploying on September 20. This supersedes the earlier local-only restriction for this release. Application code remains the reviewed implementation in `827f6ef`; deployment preparation aligns CI with Node 24 and hardens Supabase function permissions.

## Readiness

- Previous exact application build passed 394 unit tests, lint, type checking, desktop/mobile checks and the production build.
- Real-process SQLite tests rerun: 5 passed.
- Updated SQL verifier: 55 assertions passed, including a regression reproducing Supabase’s direct default grants before verifying their removal.
- Release review: approved, no material findings.
- Vercel project `operatoruplift/buffer`, Node 24.x; RPC, public Supabase configuration, server-only rate-limit secret and disabled public email flag present.
- Hosted PostgreSQL 17.6: all five report/alert tables have RLS enabled. Protected alert helpers cannot be executed by anonymous or authenticated browser roles.
- Applied `harden_alert_pipeline` (`20260919172720`) and `harden_function_permissions` (`20260919173121`). Existing alert tables were empty before migration. No scheduler or external destination was activated.

## Release verification

The Git integration deployed reviewed revision `893a1ee90f8f03f41727cff2d946c24d51af48ba` as `dpl_9B3ssbB9h7gNieyqvwDoX2BWuZLA`, `https://buffer-3b7df3vqh-operatoruplift.vercel.app`. Vercel confirmed Ready with both `https://bufferonsolana.vercel.app` and the legacy `https://buffer-lovat.vercel.app` aliases.

- All seven checked routes returned HTTP 200: `/`, `/app`, `/auth`, `/demo`, `/brand-kit`, `/manifest.webmanifest`, `/api/config`. Live reads were configured.
- Logo/favicon assets, selected hero film, all three narrated MP4s and captions returned HTTP 200.
- **26 applicable production browser cases passed** across desktop/mobile: scene travel/state, films/captions/seeking, navigation, offline arithmetic/cache privacy, installation, and local alert behavior. Two account-switch tests rejected the production URL through their deliberate localhost-only auth-mock guard; they had passed in local verification and no production auth request was sent.
- Four production landing/app screenshots at 1440×1000 and 390×844 show no page errors, failed requests, horizontal overflow or clipped controls.
- Fresh Velocity HTTP 200: two positions, USDT settlement, USD current risk context, observed slots 448476585/448476586 at `2026-09-19T17:35:10.894Z`.
- Fresh Pacifica HTTP 200: 22 positions, USD price effects, API timestamps.
- Jupiter HTTP 200: one SOL-PERP inventory-only row, explicitly unmodeled; `inventoryAvailable:false`. No price-effect total was fabricated.
- Invalid address/protocol: sanitized HTTP 400. Private/app/auth responses and APIs remain uncached.

The initial release [CI run](https://github.com/operatoruplift/buffer/actions/runs/35458412260) passed lint, type checking, 394 unit tests and build. Its non-blocking browser job exposed two outdated Jupiter assertions after the intentional prior-snapshot retention change (96 passed, 64 skipped, 2 failed). The followup aligns that test with labeled retained-state behavior and supplies non-secret fixture configuration so mocked auth/report cases run in CI, including email flows against an invalid fixture host only. Production email remains disabled. No application source change was needed. The subsequent CI run verifies those test-only corrections.

Evidence is stored locally under `work/evidence/release-2026-09-20/`: `production-api.json`, `browser-summary.json`, `browser-tests.log`, `visual-manifest.json`, screenshots and `ci-verify.log`.

## Rollback

Previous ready deployment: `dpl_F7rpB91659WtW7ZXa9s5SiLA8Mpi`, `https://buffer-mgi9ybbbx-operatoruplift.vercel.app`. Re-promote it if this release breaks app navigation, scenario arithmetic, static assets, or server routes. The additive database hardening is compatible with the previous app and should remain applied; do not remove owner controls to roll back the website.

## Deliberate limits

Public signup/recovery email and hosted external alert delivery remain disabled. Browser monitoring is manual and local; the durable SQLite worker is separate. Existing September 13 narrated films remain available with explicit historical context on the updated demo page. Jupiter remains inventory-only. The security advisor still identifies the existing anonymous/authenticated rate-limit RPC as SECURITY DEFINER; this is intentional because the server-only secret gates its use. Alert-helper findings and the mutable search-path finding were resolved.
